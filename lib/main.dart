import 'package:flutter/material.dart';
import 'package:newurllauncher/splash_screen.dart';

void main() {
  runApp(
    const MaterialApp(
      home: SplashScreen(), // Wrap the Starter widget with MaterialApp
    ),
  );
}
 